
<section class="max-w-7xl mx-auto py-12 px-4">
  <h2 class="text-2xl font-bold mb-6 text-center text-gray-800"><%= title %></h2>

  <div class="grid grid-cols-2 md:grid-cols-3 gap-8">
    <% products.forEach(prod => { %>
      <div class="bg-white p-4 rounded shadow">
        <img src="<%= prod.image %>" alt="<%= prod.name %>" class="w-full h-40 object-cover mb-2">
        <h3 class="text-lg font-semibold"><%= prod.name %></h3>
        <p class="text-gray-700">₹<%= prod.price %></p>
      </div>
    <% }) %>
  </div>
</section>
