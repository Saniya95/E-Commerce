<%- include("../partials/header", { title: "Add Product" }) %>

<section class="max-w-xl mx-auto p-6 bg-white rounded-lg shadow-md mt-10">
  <h2 class="text-2xl font-bold mb-4">🛒 Add New Product</h2>
  <form action="/admin/products/add" method="POST" enctype="multipart/form-data" class="space-y-4">
    <input name="name" required placeholder="Product Name" class="w-full border p-2 rounded" />
    <textarea name="description" placeholder="Description" class="w-full border p-2 rounded"></textarea>
    <input name="price" required type="number" placeholder="Price ₹" class="w-full border p-2 rounded" />
    <input name="category" required placeholder="Category (e.g. Men, Women)" class="w-full border p-2 rounded" />

    <div>
      <label class="block font-semibold">Upload Image</label>
      <input type="file" name="imageFile" accept="image/*" class="w-full border p-2 rounded" />
    </div>
    <div>
      <label class="block font-semibold">OR Image URL</label>
      <input type="url" name="imageUrl" placeholder="https://..." class="w-full border p-2 rounded" />
    </div>

    <button type="submit" class="w-full bg-blue-600 text-white py-2 rounded hover:bg-blue-700">➕ Add Product</button>
  </form>
</section>

<%- include("../partials/footer") %>
