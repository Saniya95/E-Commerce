<!DOCTYPE html>
<html lang="en" data-theme="light">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>

    <!-- ✅ Tailwind & Fonts -->
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
      body { font-family: 'Inter', sans-serif; }
    </style>

    <!-- ✅ AOS Animation -->
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">

    <!-- ✅ Toastify -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/toastify-js/src/toastify.min.css">

    <!-- ✅ Swiper Carousel -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css" />
  </head>

  <body class="min-h-screen flex flex-col bg-white text-gray-800 dark:bg-gray-900 dark:text-white">

    <!-- ✅ Navbar -->
    <nav class="bg-gray-800 text-white p-4 flex items-center justify-between">
      <div class="text-xl font-bold">🛒 Scratch</div>

      <ul class="flex gap-6 text-sm items-center">
        <li><a href="/" class="hover:underline">Home</a></li>
        <li><a href="/categories" class="hover:underline">Categories</a></li>
        <li>
          <% if (user) { %>
            <a href="/cart" class="hover:underline">Cart</a>
          <% } else { %>
            <a href="#" id="cartToast" class="hover:underline">Cart</a>
          <% } %>
        </li>
        <li><a href="/about" class="hover:underline">About Us</a></li>
        <li><a href="/contact" class="hover:underline">Contact</a></li>
      </ul>

      <div class="relative inline-block text-left ml-4">
        <button id="menuButton" class="text-white p-2 hover:bg-gray-700 rounded">⋮</button>
        <div id="menuDropdown" class="hidden absolute right-0 mt-2 w-44 bg-white rounded-md shadow-lg z-50 text-gray-700">
          <% if (user) { %>
            <a href="/users/profile" class="block px-4 py-2 text-sm hover:bg-gray-100">👤 Profile</a>
            <form action="/users/logout" method="POST">
              <button type="submit" class="w-full text-left px-4 py-2 text-sm text-red-600 hover:bg-gray-100">🚪 Logout</button>
            </form>
          <% } else { %>
            <a href="#" id="profileBlocked" class="block px-4 py-2 text-sm text-gray-400 cursor-not-allowed">👤 Profile</a>
            <a href="/login" class="block px-4 py-2 text-sm hover:bg-gray-100">🔑 Login</a>
            <a href="/signup" class="block px-4 py-2 text-sm hover:bg-gray-100">📝 Signup</a>
          <% } %>
        </div>
      </div>
    </nav>

    <!-- ✅ Main Content -->
    <main class="flex-1">
