// <%- include('../partials/header', { title: category.name }) %>

// <section class="max-w-7xl mx-auto py-10 px-4">
//   <h2 class="text-3xl font-bold text-gray-800 mb-6 text-center"><%= category.name %> 🛍️</h2>

//   <% if (products.length === 0) { %>
//     <p class="text-center text-gray-500">No products available in this category yet.</p>
//   <% } else { %>
//     <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6">
//       <% products.forEach(product => { %>
//         <div class="bg-white shadow rounded-lg overflow-hidden hover:shadow-xl transition duration-300">
//           <img src="<%= product.image %>" alt="<%= product.name %>" class="w-full h-56 object-cover">
//           <div class="p-4">
//             <h3 class="text-lg font-semibold text-gray-900"><%= product.name %></h3>
//             <p class="text-sm text-gray-600 mb-2"><%= product.description %></p>
//             <p class="text-blue-600 font-bold">₹<%= product.price %> 
//               <% if (product.discount) { %>
//                 <span class="text-sm text-gray-400 line-through ml-2">₹<%= (product.price / (1 - product.discount / 100)).toFixed(0) %></span>
//               <% } %>
//             </p>
//           </div>
//         </div>
//       <% }) %>
//     </div>
//   <% } %>
// </section>

// <%- include('../partials/footer') %>
